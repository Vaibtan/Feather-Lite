{
  "name": "@feather-lite/console",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Operator console: conversations, timelines, scenarios, live calls (static; deploys to Cloudflare Pages).",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "dev": "vite --port 5173",
    "build": "vite build",
    "preview": "vite preview --port 5173"
  },
  "dependencies": {
    "@feather-lite/contracts": "workspace:*",
    "@feather-lite/domain": "workspace:*",
    "effect": "^3.22.1",
    "livekit-client": "^2.21.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^8.2.1"
  }
}
