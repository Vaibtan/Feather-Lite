# Feather-Lite v2 — progress log

Plan: `docs/plans/2026-08-16-ts-effect-rebuild.md`. One line per phase milestone; newest last.

| Date | Phase | Status | Notes |
|---|---|---|---|
| 2026-08-16 | 0 Monorepo & toolchain | done | pnpm 11 workspaces, TS 5.9 strict, vitest 3 + @effect/vitest, Effect 3.22 pinned; `pnpm check` green on empty packages; docker-compose Postgres |
| 2026-08-16 | plan rev.2 | done | adversarial review (25 findings) folded in: two-mode turns, 3-phase turn tx, SSE frame protocol, forced transitions, proposal/read-back tools, tracer bullet before Phase 2 |
| 2026-08-16 | 1 Domain | done | `packages/domain`: enums, branded ids, values (money/date/tz), state machine (adjacency + override + forced), overrides (normalised, name-aware), tools + arg schemas + matrix, event union (19 types), replay reducer, transcript, pre-call policy, time helpers, context gate, scripts, turn contract; 92 tests |
| 2026-08-16 | 1.5 Voice tracer bullet | done | D2 proven on @livekit/agents 1.6.4 + LiveKit Cloud: text-mode harness (session.run) and a 51.8 s fully automated voice call (STT, streamed llmNode reply, barge-in truncation, interruptible read-back, durable-then-speak confirmation, hangup via deleteRoom). 14 findings in `2026-08-16-phase-1.5-tracer-findings.md`; read-backs now interruptible + fully-heard guard |
| 2026-08-16 | 2a Control plane core | done | Effect services: ContextBuilder, TurnDecider (scripted/failing/static), Orchestrator (3-phase turn, two-mode streaming, overrides, tools w/ idempotency + fully-heard guard, finalize, signals, no-input), CallControl, Workflow.startCall, Scheduling, Outbox, Queries, ScenarioRunner. **20/20 scenarios green on real Postgres** + concurrency (409 / supersede) + 20 parser unit tests |
| 2026-08-16 | 2b Workers + seed | done | scheduled-action worker (retry re-dial, TCPA reschedule to next 08:00 local), outbox worker (summary/evaluation/vector-stub + OUTBOX_PROCESSED), idempotent seed with real orchestrator-generated history; tests isolated on `feather_lite_test` |
| 2026-08-16 | 3 HttpApi + Node server | done | `packages/contracts` HttpApi (18 routes, OpenAPI at /docs), handlers with typed error mapping, SSE turn stream via TurnRunner (detached fibers, replay on reconnect), bearer/rate-limit/daily-cap middleware, CORS, `apps/server` with in-process schedulers; verified live: start/turn(SSE)/simulate/409/422/scenario/status; CI workflow |
| 2026-08-16 | 4 Real LLM | done | `LlmClient` seam (OpenAI streaming + recording fake), `OpenAITurnDecider` (two-mode streaming, domain tools from Effect JSON schemas, pseudo-tools for state suggestions, named failures), prompt builder, `Tracing` (Langfuse/noop/recording); request-body leak test through the real orchestrator; live GPT-4.1 conversation verified end-to-end via API (PTP recorded, TTFT 1.2-2s). Policy tweak: pending CALLBACK blocks a start, RETRY is superseded |
| 2026-08-16 | 5 Voice worker | done | production `apps/voice-worker`: FeatherAgent.llmNode streams `/turn` SSE, say frames, opening_played / playout / no_input / hangup signals, AMD-gated SIP path (needs trunk), heartbeats; `POST /api/voice/sessions` bootstraps room+dispatch+token. Verified: automated real voice call through the real API + LiveKit Cloud with GPT-4.1, and with the scripted decider producing state path/tools/outcome identical to the simulation scenario (SPEC §10.5) |
| 2026-08-16 | 6 Operator console | done | `apps/console` (Vite + TS, no framework): Conversations list/detail (transcript with heard-text, event timeline, replay, actions, outbox), Simulate (streaming SSE deltas/say bubbles, no-input/hangup), Live call (browser WebRTC via `POST /api/voice/sessions`, agent audio, `lk.transcription` live transcript, ledger polling; SIP button), Scenarios matrix (run one/all, expected vs actual paths), Status (health, agent workers, seed/reset, API base + token). Verified headless against the running server: run-all 20/20, a full simulated PTP conversation, and a real browser call joining LiveKit Cloud with the agent's opening rendered live. Added `VirtualClock` (frozen for scenarios, shifted for seeded history) so replayed calls carry consistent timestamps |
| 2026-08-16 | 7 Hardening & docs | done | README (thesis, status table, architecture, run/test, live-demo pointer, honest "not built"), ADRs 0001–0005, `docs/deploy/free-tier-live-demo.md` runbook (Neon + Cloudflare Tunnel + Pages + LiveKit Build, cost sheet, smoke test), `.env.example`, root scripts (`dev`, `tunnel`, `deploy:console`, …), demo script rewritten to match the console, SPEC §19 v2 status note; durable ledger counts on `/api/system/status` + console Guardrails card; LiveKit bootstrap timeout. Not built (listed in README): PSTN trunk verification, always-on VM, latency waterfall, chaos test |
| 2026-08-21 | 8 Self-hosted media + load testing | done | Plan: `2026-08-21-local-selfhost-livekit-loadtest-spec.md`. **Phase A**: `livekit/livekit-server:v1.13.5` under a compose profile (`pnpm lk:up`) with a single-port UDP mux + `--node-ip 127.0.0.1` for Docker Desktop; `STT_TTS_PROVIDER` switch (`inference` \| `plugins` = Deepgram STT + Cartesia TTS) since LiveKit Inference is Cloud-only; SIP now fails fast when no trunk is configured. Verified on the local SFU: server smoke, worker registration, **headless voice call equivalence-green vs the simulation scenario**, and a headless-Chrome browser call with live transcript + barge-in reaching PROMISE_TO_PAY. **Phase B**: tier-1 harness (`apps/load-test`) 10/50/100/200 concurrent conversations — all correct, knee at C≈50–100, ~80–90 turns/s, saturation is the single Node process not Postgres (pool 10→40 made it slower, 33/41 backends idle-in-tx); tier-2 voice fleet 2/5/10 concurrent real calls with WAV-cached borrower lines — N=5 green (acceptance), N=10 is the laptop CPU ceiling (10/10 then 9/10; the one miss is the fully-heard read-back guard correctly refusing an unheard promise). **Phase C**: ADR 0006, `docs/loadtest/README.md`, README, `.env.example`. Finding: Cloud + `inference` failed 3/3 (Inference STT `transcriptionDelayMs` 5–18 s vs 0.2–0.5 s direct); Cloud + `plugins` passes — recorded in ADR 0006 |
| 2026-08-23 | 9 Context, latency & observability | done (5 of 6 phases landed; 1 reverted) | Plan: `2026-08-22-context-latency-fixes-and-observability-spec.md`, on the research in `2026-08-22-context-memory-and-latency-research.md`. **P1** per-turn response latency in the voice harness (borrower falls silent → agent replies), with unanswered turns reported rather than dropped; baseline mean ~2.1-2.3 s. **P2** context misses fixed at the source: transcript window 12 → 100 entries (six exchanges was losing the Mini-Miranda and every free-form borrower fact), and superseded turns excluded from the decider's transcript only — the console/outbox ledger still shows what was actually said. **P3** prompt re-laid out for OpenAI's prefix cache (static persona/RULES → transcript → volatile state/time/account → current line) plus `prompt_cache_key`; `cached_tokens` reached 1792/1920 on a long call and stays 0 on short ones, which never reach the 1,024-token floor. **P4** dropped the deprecated text EOU model *and* its explicit endpointing for the auto-provisioned audio `inference.TurnDetector` at 300/2500: tier-2 N=5 per-turn p50 2397 → 2145 ms, worker EOU delay ~780 → ~578 ms, 5/5 green. **P5 reverted**: Deepgram Flux (STTv2 + `turnDetection: "stt"`) transcribed perfectly but failed the equivalence gate 4/4 — turns commit before a non-interruptible read-back finishes playing, so the fully-heard guard correctly rejected the promise (`TOOL_REJECTED INVALID_ARGS`) and the call ended FAILED; not the endpointing (a 300 ms run failed identically); nova-3 kept. **P6** Langfuse rewritten onto the current JS SDK (`@langfuse/tracing` + `@langfuse/otel` v5, OTel-based) against a self-hosted Langfuse (`pnpm lf:up`): one session per conversation, a span per turn carrying state/tool/outcome/superseded, a nested generation with tokens and `cached_tokens`; the worker's EOU/transcription/TTS-TTFB now reach the ledger via a `turn_metrics` signal and sit next to `ttft_ms`, so one row holds the whole waterfall; console draws it per call and as fleet p50/p95. Also fixed: TTFT was measured by subtracting a VirtualClock-shifted start from a real `Date.now()`, so seeded rows carried multi-day "latencies" |
| 2026-08-23 | 10 Overnight verification & autoresearch | done | Repeated E2E runs against the new waterfall surfaced two real defects the clean runs had hidden. **Fixed**: (1) voice/sim equivalence silently depended on GPT-4.1 inferring an unstated amount — the borrower's line said "pay on Friday" vs the reference's "pay 550 on Friday", and the prompt's own "date with full balance implied" vs "only a date → ask, do not guess" contradiction made the call flow a coin flip (seen live: clarify → deaf harness → 2 no-input strikes → NO_ANSWER, equivalence FAIL); harness now speaks the reference semantics and answers an amount question once, prompt resolves date-only offers to the full balance behind the read-back gate. (2) A Deepgram TTS websocket connect hang produced a zero-audio read-back that the framework force-closed as "played in full" and the ledger recorded as heard — patched the plugin with a 4 s connect timeout (retryable, so the framework retry loop recovers in ~5 s worst case) and the worker now reports a TTS-silent turn as `heard_text:"", interrupted:true` so the fully-heard guard repeats the read-back (interrupted items exempt — measured: barge-in `tts_metrics` arrive after the truncated item). **Built**: cross-call memory (research §3.3) — SUMMARY outbox job persists a `wrap_up` into `final_outcome_metadata`; pure `priorCallNote()` renders one clamped line per prior call (promises, dispute/hardship verbatim excerpts, borrower's last words) into HISTORY; verified live in the next call's prompt. **Measured & decided**: static prefix over the 1,024-token cache floor + `prompt_cache_key` per state → cross-call GREETING cache hits, latency-neutral (kept for cost); preemptive generation REJECTED (overlap window ~150 ms, not the expected chunk); `queueSizeMs` 200 REJECTED (no onset effect); harness now also measures first-audio RMS onset (tracks the transcript metric within ~100 ms). Fleet N=5: 5/5 equivalence green, p50 3039 / p95 3916 ms (vs 2145 daytime — unattributed, re-measure; ADR 0008). 7 commits |
| 2026-08-27 | 11 Quality & SLO layer | done | Plan: `2026-08-26-quality-and-slo-layer-spec.md`. **P1** one ledger-side score model (`conversation_scores`, upsert by `(conversation, turn, name, source)`, no FK so the scenario suite can score a synthetic run id), `Tracing.score` mirroring to Langfuse sessions, `GET/POST /api/conversations/:id/scores`. **P2** the EVALUATION job's two regexes became a pure domain evaluator writing compliance + call-fact scores; positional checks, `null` where there is no evidence. **P3** provider error/retry/timeout counters per vendor with a last-error ring, plus the six failure counts ADR 0008 named. **P4** orphaned-call sweeper: worker-liveness detection (heartbeat carries served conversations) confirmed against LiveKit before finalizing, ~35-40 s, FAILED/ORPHANED as a normal CALL_CONTROL event; `chaos-orphan.ts` automates the probe the README had owed since Phase 7. **P5** outcome funnel (attempts -> connected -> right-party -> promise, each rate against the previous stage), promise ageing in the borrower's timezone, SLO verdict per latency component, all on one `GET /api/system/quality`; SPEC §17.2's last two metrics now present. **P6** WER measured by the harness against the exact text it spoke, explicit normaliser (contractions, number words, currency, spoken digit runs), fleet gate `--max-wer` 0.20 set from measurement; the pairing bug it exposed (one utterance split across two STT finals) was the real find. **P7** TTS heuristics -- zero-audio playouts and chars/second outliers beyond ±40% of the window's own median -- labelled a heuristic everywhere, with no MOS model faked. **P8** LLM judge (GPT-5.6 **Luna**, binary per dimension with a quoted evidence span, post-call in its own outbox job, model call outside the transaction, unusable output -> `judge.invalid_output`); `LlmClient` grew a non-streaming `complete()` that omits sampling params for reasoning models. **P9** console Quality view, human pass/fail label control feeding judge-vs-human agreement, per-turn scores, provider-error ring, SLO beside the fleet latency. **P10** scenario suite pass-rate score, README status rows, ADR 0009. Verified live end to end: a real call judged by Luna in one attempt with 6 evidence-carrying scores, a human label moving agreement from null to 100%, 20/20 scenarios scored, and the scenario suite scored. **Fleet re-measure did not reproduce August's 5/5**: N=5 gave 3/5 with the WER gate firing on two empty transcripts, while N=2 was 2/2 clean and a single call passed throughout. Diagnosed as host starvation, not regression -- every locally-computed stage 3-5x slower than baseline while the purely-remote TTS first byte was unchanged; details in `docs/loadtest/README.md`. Two real bugs found by this work's own verification and fixed: a superseded turn was being counted as a silent playout (22% -> the true 1-in-18), and every per-turn score was being rejected by Langfuse for naming an observation without its trace. 6 commits |
| 2026-08-28 | 12 Efficiency & observability hardening | in progress (Phases 0-4 done, 5 part, 6 most) | Plan: `2026-08-27-efficiency-and-observability-hardening-spec.md`, on the audit in `2026-08-27-efficiency-audit-findings.md`. Numbers and how to read them: the 2026-08-28 section of `docs/loadtest/README.md`. **Phase 0 (D1)** built the instruments and took the baselines: a resource sampler naming every process in both trees, an open-loop soak, the fleet's borrowers moved into their own process (W8 — 26 % of the machine at N=5), and the per-core budget defined once. **Phase 1-2 (D2)** fixed sixteen observability defects — one percentile rule, an SLO window segmented by channel and decider with a minimum sample, a funnel that counts only finished calls and shows orphaned as a subset of failed, `latency.slo_pass` persisted per call, a scheduled voice re-dial that actually dispatches (and `NEVER_SERVED` for a call that never had a worker, which took the chaos p95 from 308 s back to the ~40 s class), Langfuse ingestion failures counted instead of swallowed, harness scores naming a turn that exists, rate-limit rejections counted, and the status page answering in bounded time. **Phase 3 (D3)** made the process observable: event-loop delay, RSS/heap, GC, pg-pool depth and per-loop liveness on `/api/system/status`, a `/readyz` that fails when a background loop stops ticking, the same numbers scraped at `GET /metrics` in Prometheus format (from the same snapshot, so the two surfaces cannot disagree — which is why `collectDefaultMetrics` is deliberately not called), `conversation_id`/`turn_id` on every log line of the turn path, and `TRACE_REDACT_ACCOUNT_DATA` masking amounts, dates and identifiers in every exported span body. **Phase 4 (D4)** cut the worker: the main process stops loading a 69 MB inference addon to check that it exists (**−874 MB**, and −740 MB again under the bundle, which is what settles the patch), an explicit `activeJobs / WORKER_MAX_JOBS` load function *and* worker-side admission control — the load function alone could not shed a burst, measured: three simultaneous calls all served at `max_jobs=2` — four warm job slots (five calls now finish 3.9 s apart instead of 14.6 s), `UV_THREADPOOL_SIZE` sized for the EOU model, real per-job memory limits, and 165 MB of ONNX runtimes gone with an unused plugin. The worker's mode, load and limits are on the heartbeat and the status page, and the fleet harness refuses to measure a `dev`-mode worker. **Phase 5 (D6, part)**: both apps build to a single-file esbuild bundle and start with `node` — job-process module load 2 659 → 1 834 ms, the worker tree 2 409 → 1 620 MB, and 13 % less CPU per call-minute at N=5. Dockerfiles, compose limits and `stack:quiet` are **not done**. **Phase 6 (D5b then D5)**: `pg_stat_statements` preloaded and its top-10 in every load report, `fillfactor`/autovacuum on the hot tables, two indexes the ranking asked for (one of them 40 % of all database time, a seq scan nobody had counted), event append folded into one statement, the prompt context into one query, a work-conserving outbox (283 jobs in 74 s → 103 in 4 s) and a bounded turn-retention map (46 → 26 MB/min of growth under soak). **43.6 → 31.7 Postgres statements per completed turn, −27 %.** Two things were deliberately **not** built, both on evidence: `listEventsUnchecked` (a self-taken CPU profile puts all schema decoding at ~2.4 % of busy time — there is no hot spot) and `conversations.next_sequence_no` (the aggregate it would remove costs 0.045 ms; the round trip was the cost, and the single-statement append already removes it). Two measurement bugs found and fixed on the way: a load run starting inside the previous run's outbox backlog reported the drain as its throughput, and `node --cpu-prof` cannot be used on Windows at all — so the server profiles itself behind `PROFILE_SECONDS`. **Still open**: Phase 5 containers, Phase 7 (native VAD, resample A/B, `tts_connect_ms`), Phase 8 (two processes + leader election), Phase 9 (N=10 acceptance, ADR 0010, README). 17 commits |
