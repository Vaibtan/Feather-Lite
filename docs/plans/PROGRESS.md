# Feather-Lite v2 — progress log

Plan: `docs/plans/2026-08-16-ts-effect-rebuild.md`. One line per phase milestone; newest last.

| Date | Phase | Status | Notes |
|---|---|---|---|
| 2026-08-16 | 0 Monorepo & toolchain | done | pnpm 11 workspaces, TS 5.9 strict, vitest 3 + @effect/vitest, Effect 3.22 pinned; `pnpm check` green on empty packages; docker-compose Postgres |
| 2026-08-16 | plan rev.2 | done | adversarial review (25 findings) folded in: two-mode turns, 3-phase turn tx, SSE frame protocol, forced transitions, proposal/read-back tools, tracer bullet before Phase 2 |
| 2026-08-16 | 1 Domain | done | `packages/domain`: enums, branded ids, values (money/date/tz), state machine (adjacency + override + forced), overrides (normalised, name-aware), tools + arg schemas + matrix, event union (19 types), replay reducer, transcript, pre-call policy, time helpers, context gate, scripts, turn contract; 92 tests |
| 2026-08-16 | 1.5 Voice tracer bullet | done | D2 proven on @livekit/agents 1.6.4 + LiveKit Cloud: text-mode harness (session.run) and a 51.8 s fully automated voice call (STT, streamed llmNode reply, barge-in truncation, interruptible read-back, durable-then-speak confirmation, hangup via deleteRoom). 14 findings in `2026-08-16-phase-1.5-tracer-findings.md`; read-backs now interruptible + fully-heard guard |
| 2026-08-16 | 2a Control plane core | done | Effect services: ContextBuilder, TurnDecider (scripted/failing/static), Orchestrator (3-phase turn, two-mode streaming, overrides, tools w/ idempotency + fully-heard guard, finalize, signals, no-input), CallControl, Workflow.startCall, Scheduling, Outbox, Queries, ScenarioRunner. **20/20 scenarios green on real Postgres** + concurrency (409 / supersede) + 20 parser unit tests |
| 2026-08-16 | 2b Workers + seed | done | scheduled-action worker (retry re-dial, TCPA reschedule to next 08:00 local), outbox worker (summary/evaluation/vector-stub + OUTBOX_PROCESSED), idempotent seed with real orchestrator-generated history; tests isolated on `feather_lite_test` |
| 2026-08-16 | 3 HttpApi + Node server | done | `packages/contracts` HttpApi (18 routes, OpenAPI at /docs), handlers with typed error mapping, SSE turn stream via TurnRunner (detached fibers, replay on reconnect), bearer/rate-limit/daily-cap middleware, CORS, `apps/server` with in-process schedulers; verified live: start/turn(SSE)/simulate/409/422/scenario/status; CI workflow |
| 2026-08-16 | 4 Real LLM | done | `LlmClient` seam (OpenAI streaming + recording fake), `OpenAITurnDecider` (two-mode streaming, domain tools from Effect JSON schemas, pseudo-tools for state suggestions, named failures), prompt builder, `Tracing` (Langfuse/noop/recording); request-body leak test through the real orchestrator; live GPT-4.1 conversation verified end-to-end via API (PTP recorded, TTFT 1.2-2s). Policy tweak: pending CALLBACK blocks a start, RETRY is superseded |
| 2026-08-16 | 5 Voice worker | done | production `apps/voice-worker`: FeatherAgent.llmNode streams `/turn` SSE, say frames, opening_played / playout / no_input / hangup signals, AMD-gated SIP path (needs trunk), heartbeats; `POST /api/voice/sessions` bootstraps room+dispatch+token. Verified: automated real voice call through the real API + LiveKit Cloud with GPT-4.1, and with the scripted decider producing state path/tools/outcome identical to the simulation scenario (SPEC §10.5) |
| 2026-08-16 | 6 Operator console | done | `apps/console` (Vite + TS, no framework): Conversations list/detail (transcript with heard-text, event timeline, replay, actions, outbox), Simulate (streaming SSE deltas/say bubbles, no-input/hangup), Live call (browser WebRTC via `POST /api/voice/sessions`, agent audio, `lk.transcription` live transcript, ledger polling; SIP button), Scenarios matrix (run one/all, expected vs actual paths), Status (health, agent workers, seed/reset, API base + token). Verified headless against the running server: run-all 20/20, a full simulated PTP conversation, and a real browser call joining LiveKit Cloud with the agent's opening rendered live. Added `VirtualClock` (frozen for scenarios, shifted for seeded history) so replayed calls carry consistent timestamps |
| 2026-08-16 | 7 Hardening & docs | done | README (thesis, status table, architecture, run/test, live-demo pointer, honest "not built"), ADRs 0001–0005, `docs/deploy/free-tier-live-demo.md` runbook (Neon + Cloudflare Tunnel + Pages + LiveKit Build, cost sheet, smoke test), `.env.example`, root scripts (`dev`, `tunnel`, `deploy:console`, …), demo script rewritten to match the console, SPEC §19 v2 status note; durable ledger counts on `/api/system/status` + console Guardrails card; LiveKit bootstrap timeout. Not built (listed in README): PSTN trunk verification, always-on VM, latency waterfall, chaos test |
| 2026-08-21 | 8 Self-hosted media + load testing | done | Plan: `2026-08-21-local-selfhost-livekit-loadtest-spec.md`. **Phase A**: `livekit/livekit-server:v1.13.5` under a compose profile (`pnpm lk:up`) with a single-port UDP mux + `--node-ip 127.0.0.1` for Docker Desktop; `STT_TTS_PROVIDER` switch (`inference` \| `plugins` = Deepgram STT + Cartesia TTS) since LiveKit Inference is Cloud-only; SIP now fails fast when no trunk is configured. Verified on the local SFU: server smoke, worker registration, **headless voice call equivalence-green vs the simulation scenario**, and a headless-Chrome browser call with live transcript + barge-in reaching PROMISE_TO_PAY. **Phase B**: tier-1 harness (`apps/load-test`) 10/50/100/200 concurrent conversations — all correct, knee at C≈50–100, ~80–90 turns/s, saturation is the single Node process not Postgres (pool 10→40 made it slower, 33/41 backends idle-in-tx); tier-2 voice fleet 2/5/10 concurrent real calls with WAV-cached borrower lines — N=5 green (acceptance), N=10 is the laptop CPU ceiling (10/10 then 9/10; the one miss is the fully-heard read-back guard correctly refusing an unheard promise). **Phase C**: ADR 0006, `docs/loadtest/README.md`, README, `.env.example`. Finding: Cloud + `inference` failed 3/3 (Inference STT `transcriptionDelayMs` 5–18 s vs 0.2–0.5 s direct); Cloud + `plugins` passes — recorded in ADR 0006 |
