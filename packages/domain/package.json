{
  "name": "@feather-lite/domain",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Pure domain model for Feather-Lite: states, transitions, overrides, tools, events, replay, pre-call policy. Zero IO.",
  "exports": {
    ".": { "types": "./src/index.ts", "default": "./src/index.ts" }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "effect": "^3.22.1"
  },
  "devDependencies": {
    "@effect/vitest": "^0.30.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
